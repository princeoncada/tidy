# 3.1.0 Sync Latency Measurement Spike

## Status

Measurement status: **diagnostic measurement complete; 3.1.1 poke-delivery fix
verified by a representative trial; automated Baseline, Moderate Load, and
Burst distribution harness available for controller-run measurement**.

Controller smoke trials supplied real local-to-peer timings. The valid
configured trial showed no peer `poke_received` event and reached the peer only
through periodic pull. Source inspection then identified a private-channel REST
broadcast mismatch, fixed in 3.1.1. A post-fix representative trial confirmed the
peer now receives the poke and end-to-end latency dropped from 22,078 ms to
~4,094 ms (see "3.1.1 Fix Verification" below).

The 22,078 ms `smoke-004` figure is a single diagnostic sample (n=1), and the
post-fix ~4,094 ms figure is likewise a single representative trial. All three
Results distributions are produced by the opt-in automated two-user harness
(`npm run test:e2e:latency`), not by manual trials.

## Purpose And Boundary

Measure the existing Replicache path from a local dashboard mutation through
push, Supabase Broadcast poke, peer pull, and peer-visible render under
representative load. This spike does not change sync behavior or implement a
latency fix. Phase 3.1.1 must be scoped from the measurements rather than from
an assumed bottleneck.

The instrumentation is temporary, disabled by default, development-only, and
stores events in memory. It does not add fields to replicated data, alter wire
contracts, or record mutation payloads or user-entered text.

## Instrumented Stages

| Stage | Context | Meaning |
| --- | --- | --- |
| `local_mutation` | origin | Dashboard mutator method was invoked. |
| `push_start` | origin | The Replicache pusher began its HTTP operation. |
| `push_complete` | origin | The push returned a successful HTTP status. |
| `push_failure` | origin | The push threw or returned an error HTTP status. |
| `poke_received` | peer | The private Broadcast callback ran. |
| `pull_start` | peer | The Replicache puller began its HTTP operation. |
| `pull_complete` | peer | The pull returned a successful HTTP status. |
| `pull_failure` | peer | The pull threw or returned an error HTTP status. |
| `peer_render` | peer | The generated trial marker first appeared in the DOM. |

Each event includes a browser-session ID, manually shared trial ID, epoch
milliseconds for same-machine cross-context comparison, monotonic milliseconds
for within-context intervals, and limited safe metadata such as mutation name,
entity ID, request ID, counts, and HTTP status.

## Gate And Development API

The gate is off unless all of these conditions hold:

- The build is not production.
- Browser local storage is available.
- `tidy:sync-latency-spike` equals `1` before the page loads.

Enable it separately in each measurement profile from browser DevTools, then
reload:

```js
localStorage.setItem("tidy:sync-latency-spike", "1");
location.reload();
```

After reload, `window.__tidySyncLatency` exposes only:

- `startTrial(trialId)` - activates a shared trial and returns its generated
  marker.
- `stopTrial()` - stops the current trial and render observer.
- `events()` - returns a copy of buffered events.
- `clear()` - clears the bounded in-memory buffer.
- `exportJson()` - exports buffered events as formatted JSON.

Disable the spike and reload both profiles after measurement:

```js
localStorage.removeItem("tidy:sync-latency-spike");
location.reload();
```

The disabled mutation hook returns Replicache's original mutator object, and
all event functions are no-ops.

## Measurement Environment

Record these fields for the actual run:

| Field | Value |
| --- | --- |
| Date/time and timezone | Pending |
| Commit | Pending |
| Browser and version | Pending |
| OS | Pending |
| Local app mode | Pending |
| Supabase region/project | Pending |
| Network connection | Pending |
| Origin profile/account | Pending |
| Peer profile/account and effective role | Pending |

The diagnostic runs used Windows, two authenticated browser profiles on the
same machine, an owner origin, and a shared editor peer. Browser version,
network details, and Supabase region were not supplied.

Use two authenticated browser profiles on the same machine. Use an owner in the
origin profile and a different editor account in the peer profile, both viewing
the same shared list. Same-machine execution keeps the two `Date.now()` clocks
comparable without introducing device clock skew. Use a disposable workspace;
do not load-test production data.

Keep both dashboard tabs visible. Background-tab throttling can distort pull and
render timing. Record whether DevTools was open and keep that choice consistent
for every scenario.

## Trial Procedure

This is the historical manual protocol. The automated harness now drives the
same gate and event API in two authenticated browser contexts and is the data
source for the Baseline, Moderate Load, and Burst Results tables.

1. Enable the gate in both profiles and reload both dashboards.
2. Prepare the scenario's workspace size, then wait until both profiles show the
   same state and network activity is idle.
3. Run five warm-up trials. Discard them from reported percentiles.
4. Before each measured mutation, clear both event buffers.
5. Choose a unique trial ID such as `baseline-001`.
6. In the peer profile first, start the trial and retain the returned marker:

   ```js
   window.__tidySyncLatency?.clear();
   window.__tidySyncLatency?.startTrial("baseline-001");
   ```

7. Start the same trial ID in the origin profile. The returned marker must match
   the peer marker.
8. In the origin UI, rename the selected shared list to the returned marker.
   The marker is generated instrumentation text, not user content.
9. Wait for the peer profile to display the marker. Do not manually refresh.
10. Stop the trial in both profiles and export each event buffer:

    ```js
    window.__tidySyncLatency?.stopTrial();
    copy(window.__tidySyncLatency?.exportJson());
    ```

11. Store the two JSON exports under the trial ID outside the repository until
    aggregate results are calculated. Raw run logs may contain internal entity
    IDs and should not be committed.
12. Repeat until there are at least 30 valid post-warm-up samples per scenario.
    Count failed or incomplete trials; do not silently discard them.

If a trial has no `poke_received` but later renders after a periodic pull, mark
it as a missed-poke/self-heal observation. Do not combine it with poke-path
percentiles.

## Representative Scenarios

| Scenario | Workspace shape | Mutation pattern | Required samples |
| --- | --- | --- | ---: |
| Baseline | Up to 5 lists and 50 items | One shared-list rename per trial | 30 |
| Moderate load | 10 lists and about 200 items | One shared-list rename per trial | 30 |
| Burst | Moderate-load workspace | 10 sequential unique renames, treated as separate correlated trials | 30 bursts |

For the burst scenario, report both individual mutation latency and time until
the final marker renders. Keep the mutation cadence fixed and record it with the
results.

## Metric Calculation

For each valid trial, join origin and peer events by `trialId`.

| Metric | Calculation |
| --- | --- |
| Local-to-push | origin `push_start.wallClockMs` - origin `local_mutation.wallClockMs` |
| Push duration | origin `push_complete.monotonicMs` - origin `push_start.monotonicMs` |
| Push-to-poke | peer `poke_received.wallClockMs` - origin `push_complete.wallClockMs` |
| Poke-to-pull | peer `pull_start.monotonicMs` - peer `poke_received.monotonicMs` |
| Pull duration | peer `pull_complete.monotonicMs` - peer `pull_start.monotonicMs` |
| Pull-to-render | peer `peer_render.monotonicMs` - peer `pull_start.monotonicMs` |
| Pull-complete-to-render | peer `peer_render.monotonicMs` - peer `pull_complete.monotonicMs` |
| End-to-end | peer `peer_render.wallClockMs` - origin `local_mutation.wallClockMs` |

Calculate p50 and p95 with nearest ranks `ceil(0.50 * sampleCount)` and
`ceil(0.95 * sampleCount)`. Also report maximum, valid sample count, failure
count, and observed outliers for every metric. Preserve the same calculation
method for the 3.1.1 before/after comparison.

## Results

All values are milliseconds. Run `npm run test:e2e:latency` to produce local
artifacts for all scenarios (`SYNC_LATENCY_SCENARIO=all` is the default), or set
it to `baseline`, `moderate`, or `burst` for one scenario. Set
`SYNC_LATENCY_WRITE_REPORT=1` to
replace each selected scenario's table between its fill markers. Burst uses 10
correlated renames per burst with a 5,000 ms start-to-start cadence target
(`SYNC_LATENCY_BURST_CADENCE_MS` overrides it), and records configured plus
actual cadence in the artifact. Do not hand-fill unmeasured values.

### Diagnostic Smoke Trials

`smoke-003` proved the periodic-pull fallback while the local
`SUPABASE_SERVICE_ROLE_KEY` was absent. It is an environment-control observation
and is excluded from the configured result below. After the key was added and
the development server restarted, `smoke-004` still produced no peer
`poke_received` event.

| Trial | Local-to-push | Push duration | Push-complete-to-peer-pull | Peer pull | Pull-complete-to-render | End-to-end |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| smoke-003 (missing key; excluded) | 166 | 1,833 | 18,419 | 1,655 | 463 | 22,536 |
| smoke-004 (configured) | 155 | 2,046 | 18,168 | 1,325 | 384 | 22,078 |

Both trials had zero observed `poke_received` events. In `smoke-004`, the peer
pull began 18,168 ms after the successful push and the marker rendered 1,709 ms
after that pull began. This is a missed-poke/self-heal result, not a valid
poke-path percentile sample.

### 3.1.1 Fix Verification (post-fix representative trial)

After the 3.1.1 fix (`private: true` on the REST broadcast), a representative
two-profile trial (`baseline-001`) confirmed poke delivery is restored. Values
are milliseconds; this is a single representative trial (n=1), with the full
distribution deferred to the automated harness phase.

| Stage | ms |
| --- | ---: |
| Origin local-to-push | 199 |
| Origin push duration | 1,852 |
| Origin push-complete to peer `poke_received` | ~0 |
| Peer poke to pull-start | 0 |
| Peer pull duration | 1,675 |
| Peer pull-complete to render | 372 |
| End-to-end (origin mutation to peer render) | 4,094 |

The peer fired a real `poke_received` event at ~push completion (vs. zero in the
pre-fix smoke trials), eliminating the 18,168 ms missed-poke wait. End-to-end
fell from 22,078 ms to 4,094 ms (about 5.4x faster); the remaining cost is the
push and pull HTTP round-trips, which are out of 3.1.1 scope.

### Baseline

<!-- SYNC_LATENCY_RESULTS:BASELINE:START -->
| Metric | p50 | p95 | Max | Valid n | Failures |
| --- | ---: | ---: | ---: | ---: | ---: |
| Local-to-push | 429 | 518 | 562 | 30 | 0 |
| Push duration | 1244 | 1455 | 1484 | 30 | 0 |
| Push-to-poke | -31 | -2 | -1 | 30 | 0 |
| Poke-to-pull | 0 | 0 | 0 | 30 | 0 |
| Pull duration | 1501 | 1581 | 1585 | 30 | 0 |
| Pull-to-render | 1566 | 1635 | 1657 | 30 | 0 |
| Pull-complete-to-render | 62 | 83 | 89 | 30 | 0 |
| End-to-end | 3192 | 3355 | 3386 | 30 | 0 |
<!-- SYNC_LATENCY_RESULTS:BASELINE:END -->

### Moderate Load

<!-- SYNC_LATENCY_RESULTS:MODERATE:START -->
| Metric | p50 | p95 | Max | Valid n | Failures |
| --- | ---: | ---: | ---: | ---: | ---: |
| Local-to-push | 392 | 410 | 416 | 30 | 0 |
| Push duration | 1244 | 1315 | 1396 | 30 | 0 |
| Push-to-poke | -8 | 2 | 11 | 30 | 0 |
| Poke-to-pull | 0 | 0 | 0 | 30 | 0 |
| Pull duration | 1525 | 1586 | 1596 | 30 | 0 |
| Pull-to-render | 1579 | 1643 | 1653 | 30 | 0 |
| Pull-complete-to-render | 55 | 59 | 60 | 30 | 0 |
| End-to-end | 3197 | 3295 | 3358 | 30 | 0 |
<!-- SYNC_LATENCY_RESULTS:MODERATE:END -->

### Burst

<!-- SYNC_LATENCY_RESULTS:BURST:START -->
| Metric | p50 | p95 | Max | Valid n | Failures |
| --- | ---: | ---: | ---: | ---: | ---: |
| Individual end-to-end | 3244 | 3406 | 3554 | 300 | 0 |
| Final marker render | 63160 | 63779 | 63808 | 30 | 0 |
<!-- SYNC_LATENCY_RESULTS:BURST:END -->

`Final marker render` spans the full 10-rename burst and includes the configured inter-rename cadence (`SYNC_LATENCY_BURST_CADENCE_MS`, default 5,000 ms per rename); it is a burst-duration metric, not per-change sync latency. The per-change sync cost is `Individual end-to-end` (p50 ~3,244 ms), consistent with Baseline and Moderate.
Observed outliers and failures: zero failures across Baseline (n=30), Moderate (n=30), and Burst (300 individual renames / 30 bursts); no outliers beyond the distributions above.

## Measurement Limitations

- Epoch timestamps are comparable only because both profiles run on one
  machine. Do not use this protocol across unsynchronized devices.
- `local_mutation` is the call into the Replicache mutator surface, not the
  physical pointer event that initiated it.
- `pull_complete` marks completion of the puller's HTTP operation. Replicache
  still has to apply the patch and notify subscribers afterward; `peer_render`
  captures the user-visible end of that remaining interval.
- `peer_render` observes the generated marker's first appearance in DOM text.
  It deliberately limits the measured mutation to a visible rename and does not
  generalize to non-text state changes.
- Replicache can coalesce mutations into one push. `mutationCount` identifies
  that condition, but individual push duration cannot then be attributed to one
  mutation.
- Periodic pulls can occur while a trial is active. Use stage ordering relative
  to `poke_received` and classify trials without a poke separately.
- Browser scheduling, DevTools, background throttling, network variability, and
  external Supabase/Postgres load remain sources of variance.

## Bottleneck Hypothesis

The dominant observed interval is the missing-poke wait before periodic pull:
18,168 ms in the configured `smoke-004` trial, or 82% of its 22,078 ms
end-to-end latency. Push took 2,046 ms, peer pull took 1,325 ms, and render after
pull completion took 384 ms.

The delivery defect is supported by runtime and source evidence:

- The peer received no `poke_received` event after a successful HTTP 200 push.
- Adding the previously missing service-role environment key did not change the
  outcome.
- `/api/replicache/push` correctly resolves all users with access to affected
  lists and invokes `pokeUser()` for each recipient.
- The browser subscribes with `private: true`.
- `pokeUser()` sends a REST broadcast message without `private: true`, while the
  installed Supabase Realtime `httpSend` contract includes that field for
  private-channel broadcasts.
- `pokeUser()` currently ignores non-success HTTP responses, masking REST
  delivery rejection.

Therefore the primary 3.1.1 hypothesis is that the server emits a public REST
broadcast that does not reach the private per-user subscription. Periodic pull
then self-heals the peer state tens of seconds later.

## Recommended 3.1.1 Scope

Keep 3.1.1 limited to restoring and proving private poke delivery:

1. Include `private: true` in server REST broadcast messages to match the
   private client subscription.
2. Preserve best-effort sync semantics while making non-success REST responses
   observable and unit-testable instead of treating every resolved fetch as a
   successful send.
3. Extend poke tests to assert the private flag and non-success response path.
4. Re-run this protocol with at least 30 valid samples for baseline,
   moderate-load, and burst scenarios, then report p50/p95/max and compare them
   with the 22,078 ms periodic-fallback diagnostic baseline.

Do not change pull intervals, Replicache contracts, recipient authorization, or
introduce a second sync transport in 3.1.1.

## Instrumentation Removal

After the report contains accepted measurements, remove or explicitly retain
the gate before closing the spike. Full removal consists of:

1. Remove `lib/sync/sync-latency-spike.ts`.
2. Restore `hooks/useDashboardMutations.ts` to return `rep.mutate` directly.
3. Remove the measured pusher/puller wrappers from
   `lib/sync/replicache/client.ts`; retain the existing correction behavior.
4. Remove poke recording and the development API lifecycle from
   `components/ReplicacheProvider.tsx`.
5. Remove `tests/unit/sync-latency-spike.test.ts`.
6. Keep this report as the durable evidence for 3.1.1.

If the instrumentation remains temporarily gated for the 3.1.1 before/after
comparison, record that decision here and remove it when 3.1.1 closes.

3.1.1 decision (recorded, superseded): the gated instrumentation is RETAINED.
The planned removal above is NO LONGER performed at 3.1.1 close. The 3.1.2
automated two-user sync-latency harness consumes the full retained event set as
its dev-only data source and produces the Baseline, Moderate Load, and Burst
distributions. The puller wrapper in `lib/sync/replicache/client.ts` is retained
for the same reason. Removal steps 1-5 above remain deferred for a later re-scope
after future collaboration measurements no longer need the instrumentation.
